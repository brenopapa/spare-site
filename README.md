# 🍃 Spare — Site

Landing page for the [Spare](https://chromewebstore.google.com/detail/spare/YOUR_EXTENSION_ID) Chrome extension.

This repo is a single `index.html` — no build step, no dependencies. Just the public-facing site that lives at the extension's store listing URL and hosts the privacy policy.

---

**The extension itself lives elsewhere.** This repo is only the site.
